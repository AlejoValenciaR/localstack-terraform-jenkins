# Optional defaults for scripts/k8s_mail_env.sh.
# Do not put SMTP credentials or private keys in this file.

K8S_ACCESS_MODE=ssh-docker
K8S_NAMESPACE=hello-spring
K8S_DEPLOYMENT=hello-spring
K8S_REMOTE_HOST=68.155.156.136
K8S_REMOTE_PORT=22
K8S_REMOTE_USER=azureuser
# Leave blank to auto-detect the k3d server container on the Azure VM.
K8S_REMOTE_CONTAINER=
K8S_MAIL_SECRET_NAME=mail-secret
K8S_MAIL_CONFIGMAP_NAME=mail-config
